import React from 'react';
import UnAuthHeader from "../../components/Header/UnAuthHeader";

const Home = () => {
    return (
        <div>
            <UnAuthHeader />
        </div>
    );
};

export default Home;