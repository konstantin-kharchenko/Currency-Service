import React from 'react';
import CustomHeader from "../components/CustomHeader";

const Home = () => {
    return (
        <div>
            <CustomHeader />
        </div>
    );
};

export default Home;